<?xml version="1.0"?>
<ruleset name="Custom Apex Ruleset"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 
                             http://pmd.sourceforge.net/ruleset_2_0_0.xsd">

    <description>
        Custom ruleset for Apex with specific focus on CRUD operations.
    </description>

    <!-- Reference the existing ruleset containing the rule -->
    <rule ref="rulesets/apex/security.xml">
        <!-- Include the specific rule if it's defined within the referenced ruleset -->
        <properties>
            <property name="name" value="ApexCRUDViolation"/>
        </properties>
    </rule>

    <!-- Alternatively, if the rule is not found, you may define it directly (not preferred if already available in a ruleset) -->
    <!--
    <rule name="ApexCRUDViolation">
        <description>Checks for CRUD violations in Apex classes.</description>
        <priority>2</priority>
    </rule>
    -->

</ruleset>
